# CLAUDE.md 模板

本模板用于在项目根目录创建 CLAUDE.md 文件，为 Claude Code 提供项目上下文和指导。

## 基本结构

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview
项目简介，描述项目是什么、做什么用

## Common Commands
常用命令列表

## Architecture
系统架构说明

## Key Files
关键文件说明

## Dependencies
依赖环境说明
```

## 完整示例

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

这是一个 **[项目名称]** 项目，用于 **[主要用途]**.

- `src/` - 源代码目录
- `config/` - 配置文件
- `tests/` - 测试目录

## Common Commands

### 开发
```bash
# 安装依赖
pip install -r requirements.txt

# 运行开发服务器
python manage.py runserver

# 运行测试
pytest tests/
```

### 构建
```bash
# 构建项目
python setup.py build

# 打包
python setup.py sdist
```

## Architecture

### 目录结构
- `src/` - 主要源代码
- `lib/` - 库文件
- `bin/` - 可执行脚本

### 核心模块
1. **Module A** (src/a.py) - 模块A说明
2. **Module B** (src/b.py) - 模块B说明

## Dependencies

| Package | Version | 用途 |
|---------|---------|------|
| python | >=3.8 | 运行环境 |
| numpy | >=1.20 | 数值计算 |
| pandas | >=1.3 | 数据处理 |

## 调试技巧

- 使用 `python -v` 查看详细输出
- 检查 `logs/` 目录的日志文件
- 环境变量 `DEBUG=true` 开启调试模式
```

## 使用说明

1. 复制此模板到项目根目录
2. 根据实际项目修改内容
3. 根据需要添加特定领域的说明

## 注意事项

- 保持命令的时效性（版本号、路径等）
- 目录结构变更时更新 CLAUDE.md
- 重要配置信息可以添加但避免敏感内容
